\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    >=Stealth,
    box/.style={
        draw, rounded corners, minimum width=2.6cm, minimum height=0.8cm,
        align=center, font=\sffamily\bfseries, drop shadow,
        fill=cyan!20
    },
    root/.style={box, fill=orange!20, minimum width=3.2cm}
]

% 右侧节点
\node[box] (agent) at (6.5,  1.5) {Agent};
\node[box] (env)   at (6.5,  0.0) {Environment};
\node[box] (state) at (6.5, -1.8) {State};
\node[box] (action) at (6.5, -3.3) {Action};
\node[box] (reward) at (6.5, -4.8) {Reward};

% 大括号（振幅 8pt）
\draw[decorate, decoration={brace, amplitude=8pt, mirror}, 
      thick, blue!60!black] 
      (agent.west) -- (env.west)
      node[midway, left=12pt, font=\sffamily\bfseries, text=blue!60!black] 
      {Two Components};

\draw[decorate, decoration={brace, amplitude=8pt, mirror}, 
      thick, blue!60!black] 
      (state.west) -- (reward.west)
      node[midway, left=12pt, font=\sffamily\bfseries, text=blue!60!black] 
      {Three Elements};

% 大括号中点（括号路径的中点）
\coordinate (compMid) at ($(agent.west)!0.5!(env.west)$);
\coordinate (elemMid) at ($(state.west)!0.5!(reward.west)$);

% 箭头终点向左偏移 3cm（避开大括号线）
\coordinate (compTarget) at ($(compMid) + (-3,0)$);
\coordinate (elemTarget) at ($(elemMid) + (-3,0)$);

% 根节点垂直居中于两个目标点
\coordinate (midHeight) at ($(compTarget)!0.5!(elemTarget)$);
\node[root] (rl) at (0,0 |- midHeight) {Reinforcement\\Learning};

% 直线箭头（从根节点右侧直接指向目标点）
\draw[->, thick, orange!60!black] (rl.east) -- (compTarget);
\draw[->, thick, orange!60!black] (rl.east) -- (elemTarget);

\end{tikzpicture}
\caption{Basic Elements of RL.}
\label{fig:rl_basic_elements}
\end{figure}
